# Data & model weights — too large for git
data/
*.pt
*.pth
*.onnx
*.ckpt
*.npz
*.mp4
*.mkv

# Regenerable eval caches (results CSVs are kept; caches are not)
eval/*.parquet
eval/*.pkl
runs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Tooling
.DS_Store
*.log
wandb/
runs/

# NDA-gated SoccerNet videos and any local credentials — never commit
data/video/
*.mkv
.env
*secret*
